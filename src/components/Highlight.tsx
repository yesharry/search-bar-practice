const Highlight = () => {
  // if (searchValue !== "" && keyword.includes(searchValue)) {
  //   const matchText = input.split(new RegExp(`(${searchValue})`, "gi"));

  return (
    <div>
      {/* {matchText.map((input, index) =>
          input === searchValue ? (
            <span className=" font-bold" key={index}>
              {input}
            </span>
          ) : (
            input
          )
        )} */}
      test
    </div>
  );
  // }
};

export default Highlight;
